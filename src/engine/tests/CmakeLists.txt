cmake_minimum_required(VERSION 3.24)
add_subdirectory(test)
add_subdirectory(visual)
enable_testing()

include_directories(
  ${GTEST_DIR}/include
  ${CMAKE_SOURCE_DIR}/src
)

bomberman_add_test(
        NAME bomberman_vec_test
        SRCS vec_test.cpp
        DEPS bomberman_engine_testing
)